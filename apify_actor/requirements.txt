# Apify SDK 1.7.2 is installed in Dockerfile (no crawlee - avoids pydantic conflicts)

# Instagram API library (uses mobile API, not browser)
instagrapi==2.1.2

# Required dependencies
requests>=2.31.0
